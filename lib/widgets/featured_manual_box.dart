


import 'package:api_wrapper/api_wrapper.dart';
import 'package:flutter/material.dart';

class FeaturedManualBox extends StatelessWidget {
  final CompactManual manual;

  const FeaturedManualBox({Key key,@required this.manual}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
